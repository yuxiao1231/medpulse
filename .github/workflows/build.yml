name: Build MedPulse

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
      with:
        gradle-version: '8.4'

    - name: Decode Keystore from Secrets
      run: |
        $keystoreBytes = [System.Convert]::FromBase64String("${{ secrets.RELEASE_KEY_BASE64 }}")
        [System.IO.File]::WriteAllBytes("forandroidbuild/app/release.jks", $keystoreBytes)
      shell: pwsh

    - name: Build Release APK
      env:
        KEYSTORE_PASSWORD: password123
        KEY_ALIAS: key0
        KEY_PASSWORD: password123
      run: cd forandroidbuild && gradle assembleRelease

    - name: Upload Android Artifact
      uses: actions/upload-artifact@v4
      with:
        name: MedPulse-Android
        path: forandroidbuild/app/build/outputs/apk/release/app-release.apk

  create-draft-release:
    needs: build-android
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
    - name: Download Android Artifact
      uses: actions/download-artifact@v4
      with:
        name: MedPulse-Android

    - name: Create Draft Release
      uses: softprops/action-gh-release@v2
      with:
        draft: true
        name: "MedPulse Auto Release (Build ${{ github.run_number }})"
        tag_name: "v1.0.0-b${{ github.run_number }}"
        files: app-release.apk
        body: |
          **Automated Build from GitHub Actions**
          - Android APK is attached.
          - ⚠️ **Windows EXE is pending**: Please manually compile the Python 3.4 Windows XP compatible EXE and upload it to this draft release before publishing.
